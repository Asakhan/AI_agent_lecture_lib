하드코딩된 값들을 설정 파일로 분리해주세요.

[config/settings.py 생성]
다음 설정들을 포함:
- DEFAULT_MODEL = "gpt-4o-mini"
- DEFAULT_TEMPERATURE = 0.7
- MAX_TOKENS = 1000
- MAX_RETRIES = 3
- DATA_DIR = "data"
- SAVE_FORMAT = "%Y%m%d_%H%M%S"

먼저 conversation_manager.py와 main.py에서 위 값들이
하드코딩된 위치를 모두 찾아(grep 등으로) 목록화한 뒤,
config.settings를 임포트해 사용하도록 일괄 수정해주세요.
수정 후 `python -c "from config import settings; print(settings.DEFAULT_MODEL)"`와
`python -m py_compile`로 정상 동작을 확인해주세요.
